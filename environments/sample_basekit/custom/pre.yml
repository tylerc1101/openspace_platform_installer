# Optional: custom tasks to run BEFORE onboarder plan
# Fill out with same schema as plan tasks.
# Example:
# - id: hello
#   kind: bash
#   label: "Say hello"
#   script: "/usr_home/{{env}}/custom/pre/hello.sh"
